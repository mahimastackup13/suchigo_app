org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.configuration-cache=false
android.useAndroidX=true
android.enableJetifier=true
# Enable R8 full mode for better dead-code elimination (~10–20% APK size saving)
android.enableR8.fullMode=true
# Opt-in for Android 13+ predictive back gesture
android.activity.enableBackInvokedCallback=true
# Kotlin code style (official)
kotlin.code.style=official
# Ensure consistent cross-platform builds
file.encoding=UTF-8
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
